---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: omni

suites:
  - name: default
    run_list:

      - recipe[oracle-omni::installrpms]

    attributes:
      oracle-omni:
        rdbms:
          version: 11.2.0.4
          storage_type: ASM
